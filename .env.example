# ============================================
# Factor 3: Configuration in env vars
# ============================================

PORT=3002
NODE_ENV=development

# Database (Factor 4)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lostandfound
DB_USER=postgres
DB_PASSWORD=changeme

# Object Storage (Factor 4)
GCS_BUCKET=lost-and-found-photos
GOOGLE_APPLICATION_CREDENTIALS=./credentials.json

# JWT (for verifying tokens from Auth Service)
JWT_SECRET=your-secret-key-change-in-production

# Logging (Factor 11)
LOG_LEVEL=info
